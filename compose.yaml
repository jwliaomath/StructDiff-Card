services:
  structdiff-card:
    build: .
    image: structdiff-card:local
    ports:
      - "8501:8501"
    read_only: true
    tmpfs:
      - /tmp:size=256m,mode=1777
    security_opt:
      - no-new-privileges:true